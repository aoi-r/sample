# v215 ドラゴン系Excel同期土台

- `あ.xlsx` の `ドラゴン系` 列から73枚を抽出。
- 73枚すべてが `cards.json` に存在することを確認。
- 空テキストだった通常ユニット7枚を `効果なし` で補完。
- `DRAGON_TRIBE_POOL_V215` を追加し、Excel由来のドラゴン系判定をコード側でも保持。
- 詳細一覧: `data/v215_dragon_tribe_audit.csv`
